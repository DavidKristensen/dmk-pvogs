$(document).ready( function () {	// Setup - add a text input to each header cell	$('#POGs_table .filters th').each( function () {		var title = $('#example thead th').eq( $(this).index() ).text();		$(this).html( '<input type="text" placeholder="Search '+title+'" />' );	} );	var table = $('#POGs_table').DataTable( {		//fixedColumns:   true,		"bAutoWidth": true,		"lengthMenu": [[10, 25, 50, -1], [10, 25, 50, "All"]], //Drop down show options		"order": [[0,"asc"]], // Upon load orders by first column ascending		"aoColumnDefs": [ 			{ "sSortDataType": "numeric" },			{ "aTargets": 'numeric' },		],		"aaSorting": [['text','asc']], //		"sDom": '<"top"l>rt<"bottom"ipr><"clear">',	});	// Apply the search	table.columns().eq( 0 ).each( function ( colIdx ) {		$( 'input', $('.filters th')[colIdx] ).on( 'keyup change', function () {			table			.column( colIdx )			.search( this.value )			.draw();		} );	});});